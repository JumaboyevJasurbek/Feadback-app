import AddPages from "../../components/Add-pages/Add-pages";

const AddTodo = ({ feadbacks, setFeadbacks }) => {
  return <AddPages feadbacks={feadbacks} setFeadbacks={setFeadbacks} />;
};

export default AddTodo;
